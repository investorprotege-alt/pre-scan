# Generated HTML briefings (large, ephemeral — uploaded as Actions artifacts)
data/briefing_*.html

# Keep JSON data files tracked so GitHub Pages dashboard can read them
# data/*.json  ← intentionally NOT ignored

# Email config contains Gmail App Password — never commit this
config/email_config.json

# Claude Code local settings
.claude/

# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# OS
.DS_Store
Thumbs.db
desktop.ini
